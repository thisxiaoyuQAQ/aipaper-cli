# 项目开发

本 skill 用于在 `aipaper-cli` 项目初始化 / 增量规划完成后，按 vault 任务推进编码实现。当前开发阶段聚焦 TUI 全流程增量任务 `10-21`。

## 使用范围

当用户要求“进入开发阶段”“实现下一个 vault”“继续开发 TUI 全流程”“按项目开发 skill 开始编码”时，按本文执行。

本 skill 不负责重新做需求初始化；如需求变化影响架构或 vault 拆分，先回到规划文档更新，再继续开发。

## 当前项目任务集

当前待开发任务来自：

- `docs/TUI全流程增量需求.md`
- `docs/TUI全流程增量架构设计.md`
- `docs/TUI全流程详细开发流程.md`
- `docs/开发进度.md`
- `vault/10-无参数TUI启动与RootModel骨架.md` 至 `vault/21-TUI全流程测试与Windows双击验收.md`

默认按编号顺序开发，除非用户指定某个 vault。

## 开始前必读

每次开始一个 vault 前必须读取：

1. `项目备忘录.skill`
2. `docs/开发进度.md`
3. 当前 vault 文件
4. 当前 vault 的「最小上下文清单」中列出的所有文件
5. 如涉及接口契约，读取对应 `docs/interfaces/*.md`
6. 如涉及 TUI 增量，读取：
   - `docs/TUI全流程增量需求.md`
   - `docs/TUI全流程增量架构设计.md`
   - `docs/interfaces/tui.md`

## 模块选择规则

1. 如果用户指定 vault 编号，优先执行指定模块。
2. 如果用户只说“继续”，选择 `docs/开发进度.md` 中编号最低、状态为“待开发”、依赖已完成的任务。
3. 如果任务依赖未完成，先告诉用户阻塞原因，并建议应先做的 vault。
4. 不跨越依赖强行实现；允许只做可并行的准备性测试或文档，但不得声称模块完成。

当前推荐起点：

```text
vault/10-无参数TUI启动与RootModel骨架.md
```

## 标准执行流程

### 1. 读取与复述任务

- 读取必读上下文。
- 用 3-6 条简要说明本轮要改什么、涉及哪些文件、验收标准是什么。
- 若存在多个可行方案或会影响既有稳定边界，先询问用户或进入计划模式。

### 2. 标记进度

更新 `docs/开发进度.md`：

- 当前 vault 状态：`进行中`
- 如有 TUI 增量验收项也同步标记为进行中或保持待开发

如只是探索或规划，不要改成已完成。

### 3. 编写实现

实现时遵守：

- Go 代码必须 `gofmt`。
- 文件职责清晰，单文件目标 < 500 行；超过 1000 行必须拆分。
- 匹配现有命名、错误风格、测试风格。
- 优先桥接和复用既有稳定模块，不复制业务规则。
- 所有 Store 写入继续使用 `internal/store` 原子写入约定。
- TUI 不直接依赖 agentcore/litellm 原始事件，使用内部 RuntimeEvent 桥。
- API key 不得进入 UI 明文摘要、日志、run events、report、测试快照。

### 4. 自测

按 vault 的测试要求运行最小必要测试：

- 优先运行相关包测试，例如：`go test ./internal/tui/app ./internal/config`
- 涉及入口时运行：`go test ./cmd/aipaper-cli ./internal/cli`
- 阶段完成或 21 号任务运行：`gofmt`、`go test ./...`、`go build ./cmd/aipaper-cli`

如果测试失败：

- 不得标记完成。
- 报告失败命令和关键输出。
- 修复后重新测试。

### 5. 独立审查

实现完成后必须进行独立审查，作者和审查分离：

- 可调用代码审查 / verifier / 子 agent 审查当前 diff。
- 审查重点：正确性、复用边界、是否破坏 01-09 稳定模块、敏感信息泄露、测试覆盖。
- 审查发现问题时先修复，再重新测试。
- 最多进行 3 轮“审查 → 修复 → 测试”；超过 3 轮仍失败则记录阻塞和下一步。

### 6. 更新文档与进度

模块通过测试和审查后：

- 更新 `docs/开发进度.md`：当前 vault 状态为“已完成”。
- 更新 TUI 增量验收总线中对应验收项。
- 如产生 bug 或偏差，记录到 `docs/修复记录.md`；若文件不存在，创建表头。
- 如新增接口或事件契约，更新 `docs/interfaces/*.md`。
- 如改变用户可见命令 / 输出路径，更新 README 或 `docs/user-guide.md`（20 号任务前可先记录 TODO）。

### 7. 结束汇报

每轮结束必须汇报：

- 完成的 vault 编号和模块名
- 修改的关键文件
- 运行过的测试命令与结果
- 审查结果
- 未完成 / 阻塞事项
- 下一建议模块

用户未要求时不要主动 commit / push。

## 修复循环规则

如果测试或审查失败：

1. 保持当前模块状态为“进行中”或“修复中”。
2. 明确失败原因。
3. 修复最小范围问题。
4. 重新运行失败测试和相关测试。
5. 最多 3 轮；超过后停止并请求用户决策。

## TUI 增量关键禁止事项

- 不重写 `internal/tui/requirements` 校验规则。
- 不重写 `internal/tui/references` 确认和 key 冲突规则。
- 不绕过 `references/confirmed.json` 直接写作。
- 不让 TUI 直接消费第三方原始 event。
- 不把 Architect / Writer / Editor 的业务顺序硬编码在 TUI 或 Host。
- 不复制 `internal/materials`、`internal/search`、`internal/artifacts`、`internal/export` 的业务规则。
- 不在未二次确认时删除、清空或覆盖已有 run 产物。
- 不在任何 UI、日志、事件、report 中泄露完整 API key。

## TUI 增量任务速查

| 编号 | 模块 | 关键产出 | 关键测试 |
|---|---|---|---|
| 10 | 无参数TUI启动与RootModel骨架 | `internal/tui/app`、main 分流 | 无参数进 TUI，有参数 CLI 不变 |
| 11 | ConfigWizard配置向导 | `internal/tui/configwizard`、配置保存/脱敏 | 模板默认值、role runtime 解析、key 脱敏 |
| 12 | 启动状态探测与恢复入口 | StateProbe、RecoverPrompt | Store 状态映射、valid checkpoint 继续 |
| 13 | Requirements屏幕桥接与落盘 | requirements adapter | 提交写 `requirements.json`，校验失败不转场 |
| 14 | MaterialsScan与候选材料入口 | `internal/tui/materials` | 缺目录、空目录、失败/降级、BibTeX 候选保留 |
| 15 | SearchProgress与候选合并 | `internal/tui/search`、候选合并 helper | provider 失败、BibTeX + search 去重、ID 连续 |
| 16 | References屏幕桥接与确认落盘 | references adapter | confirmed/rejected/bib 写入，空确认阻塞 |
| 17 | WritingProgress四区布局与Runtime事件桥 | `internal/tui/writing`、RuntimeEvent bridge | streaming、usage、章节进度、小窗口 |
| 18 | 暂停退出与安全恢复 | Ctrl+C stop、恢复提示 | 安全停止、checkpoint 恢复、二次确认 |
| 19 | ExportSummary与完成页 | exportsummary/done | export Result 展示、docx 降级 |
| 20 | 用户文档与README快速入口 | `docs/user-guide.md`、README | 命令/路径/FAQ 一致 |
| 21 | TUI全流程测试与Windows双击验收 | E2E / 手动验收记录 | `go test ./...`、build、Windows smoke |

## 进度文件更新格式

`docs/开发进度.md` 中模块状态使用：

- `待开发`
- `进行中`
- `已完成`
- `阻塞`

如需要更细粒度，可在备注中写：`编码中`、`审查中`、`测试中`、`修复第 N 轮`。

## 修复记录格式

若 `docs/修复记录.md` 不存在，创建：

```markdown
# 修复记录

| 日期 | 模块 | 问题 | 修复 | 验证 |
|---|---|---|---|---|
```

每个修复记录必须说明验证命令或人工验收方式。

## 质量门槛

模块只有同时满足以下条件才能标记完成：

1. vault 任务清单已完成。
2. vault 测试要求已覆盖。
3. 相关测试通过。
4. 独立审查无阻塞问题。
5. 文档 / 接口 / 进度已同步。
6. 没有破坏 01-09 已验收能力。

## 默认下一步提示

当一个模块完成后，如果用户没有指定下一步，提示：

```text
建议继续下一个可开发模块：vault/XX-模块名.md
```
