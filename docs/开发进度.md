# 开发进度

> ✅ Quality Engine（23-31）阶段四复核已完成（2026-06-10），可进入开发阶段。

## 状态说明

- 待开发：尚未进入实现
- 进行中：已有部分代码或文档，但未完成验收
- 已完成：实现、测试和复核均通过
- 阻塞：需要用户决策或外部条件

## 最近需求变更

**2026-06-17**：Requirements 向导化与 MaterialsScan 自动跳过
- Requirements 改为逐步向导模式，每个字段单独询问，提供说明和示例（预填充默认值）
- 有选项的字段（语言、格式、质量模式）用数字选择（1/2/3）
- 材料目录可留空（跳过材料扫描）或输入路径（不存在自动创建）
- MaterialsScan 检测到空材料时自动跳过，直接进入 SearchProgress
- 影响模块：13-Requirements、14-MaterialsScan

**2026-06-17**：智能查询生成策略（模块 32）
- 用户反馈：主题"明朝君主哪个更厉害"搜索返回 90% 朝鲜史无关文献
- 根本原因：硬编码英文后缀"evidence literature review"破坏中文语义
- 解决方案：语言自适应查询生成、中文实体识别、多策略查询、质量评分
- 目标：中文主题相关率从 ~10% 提升到 ≥70%
- 影响模块：03-学术搜索（search 包查询生成逻辑）
- 详见：`docs/superpowers/specs/2026-06-17-intelligent-query-generation.md`

**2026-06-20**：论文质量 Skill 运行时启用
- 用户要求让 `docs/skills/paper-cli-paper-quality` 中蒸馏出的论文质量内容在程序运行时生效，并应用到写作全过程
- 解决方案：不运行时读取 docs/skills，而是本地化为可测试 `PaperQualityPolicy`，按角色注入 Coordinator/Architect/Writer/Verifier/Editor，并增强 QualityReport
- 目标：让叙事契约、证据深度、claim 支撑度、审稿式修改和人工复核提示贯穿大纲、证据、写作、验证、审稿与导出
- 影响模块：Quality Engine、角色 Runner、QualityReport、接口与文档
- 状态：✅ 39-45 全部完成并端到端验收通过（2026-06-20）：policy helper + 五角色注入 + QualityReport 增强 + E2E 非退化验收；`go build ./cmd/aipaper-cli` 与 `go test ./...` 全通过
- 详见：`docs/superpowers/specs/2026-06-20-paper-quality-skill-runtime-design.md`

## 模块任务

| 编号 | 模块 | 状态 | 依赖 | 备注 |
|---|---|---|---|---|
| 01 | 基础脚手架与配置Store | 已完成 | 无 | 已补 CLI、config、store、bootstrap 单元测试；重复 init、配置脱敏、CreateOnly、严格 JSON 读取均已覆盖 |
| 02 | 材料解析与索引 | 已完成 | 01 | 已实现材料扫描、manifest/extracted/parsed 写入、MD/TXT/BibTeX/PDF 解析、DOCX/URL/CSV 降级解析与 fixtures 测试 |
| 03 | 学术搜索与文献候选 | 已完成 | 01 | 已实现 Semantic Scholar/Crossref/arXiv/PubMed provider、HTTP mock 测试、标准化、三层去重、候选 JSON/Markdown 写入 |
| 04 | 文献确认TUI | 已完成 | 02, 03 | 已实现可测试 TUI model、选择/搜索/排序/拒绝/确认、confirmed/rejected/BibTeX 原子写入与 key 冲突处理 |
| 05 | Agent运行时与Coordinator | 已完成 | 01, 04, 07 | 已接入 agentcore/litellm，完成 Host runtime builder、Coordinator prompt/step、事实工具、事件投影、writer guard 与 mock 测试 |
| 06 | 写作产物与质量门控 | 已完成 | 05 | 已实现 `internal/artifacts` 路径/写入、质量门控、章节状态机、confirmed references 校验与单元测试 |
| 07 | StepCheckpoint恢复 | 已完成 | 01 | 已实现 latest 校验、路径/哈希失败报告、恢复 prompt、run.json.resumed_from 更新、CLI recover JSON 输出与测试 |
| 08 | 最终导出与报告 | 已完成 | 06, 07 | 已实现 `internal/export` LoadInput/ExportFinal、Markdown/Docx/references/trace/report 输出、Docx 降级和未确认引用拦截 |
| 09 | E2E验收与测试夹具 | 已完成 | 02, 03, 04, 05, 06, 07, 08 | 已新增 review-mini fixture 与 `internal/e2e` 冒烟测试，覆盖材料解析、mock 搜索、确认、重写、恢复、导出和 trace 校验 |

## TUI 全流程增量任务（10-21）

> 规划日期：2026-06-08  
> 来源：`docs/superpowers/specs/2026-06-08-tui-full-generation-user-guide-design.md`  
> 状态：✅ 增量规划已生成，待进入开发阶段

| 编号 | 模块 | 状态 | 依赖 | 备注 |
|---|---|---|---|---|
| 10 | 无参数TUI启动与RootModel骨架 | 已完成 | 01 | 已实现 `main.go` 无参数进 TUI，带参数 CLI 不变，RootModel 转场和 TUI 错误路径已覆盖 |
| 11 | ConfigWizard配置向导 | 已完成 | 10 | 已实现 OpenAI/Anthropic/Ollama/Custom 模板、项目级 `aipaper.json` 保存、role runtime 解析与 API key 脱敏 |
| 12 | 启动状态探测与恢复入口 | 已完成 | 07, 10, 11 | 已实现配置、checkpoint/run、requirements、materials、references、final 状态探测和恢复提示意图 |
| 13 | Requirements屏幕桥接与落盘 | 已完成 | 10, 12 | 已桥接既有 `internal/tui/requirements`，完成需求校验、`requirements.json` 落盘、默认 `materials/` 创建和 MaterialsScan 转场 |
| 14 | MaterialsScan与候选材料入口 | 已完成 | 02, 13 | 已实现 `internal/tui/materials` 扫描屏、缺/空目录提示、失败/降级详情、BibTeX 候选保留和 RootModel 转场 |
| 15 | SearchProgress与候选合并 | 已完成 | 03, 14 | 已实现搜索屏、材料+搜索候选合并去重、ID 重分配、最终 candidates 写入与 RootModel 转场 |
| 16 | References屏幕桥接与确认落盘 | 已完成 | 04, 15 | 已桥接既有 `internal/tui/references`，完成 LoadCandidates/ConfirmCandidates 调用、confirmed/rejected/BibTeX 写入、未确认阻塞和 WritingProgress 转场 |
| 17 | WritingProgress四区布局与Runtime事件桥 | 已完成 | 05, 06, 07, 16 | 已实现 `internal/tui/writing` 包、RuntimeEvent 桥接、四区布局 view、mock 事件测试；真实 runtime 接入已由模块 22 补齐 |
| 18 | 暂停退出与安全恢复 | 已完成 | 07, 12, 17 | 已实现 WritingProgress 两阶段停止（Ctrl+C→stopping→checkpoint saved→canceled）、RecoverPrompt 继续/重开/退出交互（重开二次确认且不删文件）、RootModel 退出确认；32 个单元测试全过 |
| 19 | ExportSummary与完成页 | 已完成 | 08, 17 | 已实现 ExportSummary/Done 包、复用 export.LoadInput/ExportFinal、RootModel 转场、37 个单元测试全过（覆盖率 98.4%/100%） |
| 20 | 用户文档与README快速入口 | 已完成 | 10-19 | 已新增 `docs/user-guide.md`（9 章 + 环境变量附录）、README 精简为快速入口；41 个文档一致性测试全过（CLI 命令、导出路径、环境变量、链接、默认模型均与代码实现一致） |
| 21 | TUI全流程测试与Windows双击验收 | 已完成 | 10-20 | 已新增 `internal/e2e/tui_flow_test.go`，覆盖 TUI mock 全流程状态契约、ConfigWizard runtime 配置解析、文档一致性；`go test ./...`、`go build ./cmd/aipaper-cli`、`go build -o aipaper-cli.exe ./cmd/aipaper-cli` 全部通过；真实 provider smoke 因无 API key 跳过并记录 |
| 22 | 真实Runtime接入与本机验收 | 已完成（经 BUG-20260613-01 补实） | 05, 07, 17, 18, 21 | ⚠️ 2026-06-13 模块 31 验收发现本行原"已完成"记录失实（接线从未落代码）；真实接线由 BUG-20260613-01 方案 B 于 2026-06-13 完成：WritingProgress 经 `runtime_launcher` 启动真实 Host/AgentRuntime（Writer/Editor/Architect LLM runner + architect_run/editor_run 工具）、续轮防呆、流式/usage 事件桥、Ctrl+C Abort 停止、恢复 prompt 注入；mock 全流程编排测试通过；真实 provider 冒烟与 Windows 本机验收见 Bug vault 验证记录 |

## Quality Engine 质量引擎任务（23-31）

> 规划日期：2026-06-10  
> 来源：`docs/superpowers/specs/2026-06-10-quality-engine-design.md`  
> 状态：✅ 规划已生成，阶段四复核已完成（2026-06-10），可进入开发阶段

| 编号 | 模块 | 状态 | 依赖 | 备注 |
|---|---|---|---|---|
| 23 | EvidenceTable底座 | 已完成 | 01, 02, 04 | 已实现 `internal/quality` 包：Evidence 类型/原子存储/严格读取、confirmed reference 与 depth 渐进校验、save/load 工具与结构化错误码、Markdown 快照测试；工具注册接线留给模块 24 |
| 24 | SectionQualityPlan写前约束 | 已完成 | 23, 05, 06 | 已实现 `internal/quality/sectionplan.go` 类型/校验/原子存储、save/load_section_quality_plan 工具、`internal/agent` 边界接线（DefaultTools 注册 quality 工具、`evidence_extraction`/`section_quality_plan` step 常量与 Coordinator/Architect 提示词扩展）；mock Coordinator 两个新 step 的 checkpoint 写入、中断恢复续跑与幂等回放均有测试 |
| 25 | Writer证据使用协议 | 已完成 | 24, 06 | 已实现 `contracts.Claim` 增 `evidence_ids`（omitempty 兼容旧 claims.json，`artifacts.ClaimEvidenceWarnings` 产 warning 不阻断）、`writer_run` 工具注入 `WriterChapterInput`（本章 quality plan + required evidence，缺产物降级 warning）、`agent.GuardWriterClaims`/`WriteGuardedDraftBundle` 硬校验（missing/unknown evidence、unconfirmed key 三类结构化错误码阻断落盘）、Coordinator prompt 扩展 Writer 证据协议；mock Writer 流程与兼容读取均有测试 |
| 26 | ClaimGraph写后抽取 | 已完成 | 25 | 已实现 `internal/quality/claimgraph.go` 类型/校验/原子读写、章节级增量 merge（id 续接重分配、其余章节不动）、跨章重复检测（文本规整 + Jaccard ≥ 0.8 分级风险不阻断）、`save_claim_graph`/`load_claim_graph`/`extract_chapter_claims` 工具与 `internal/agent` `claim_extraction` step 接线；mock Coordinator checkpoint 写入与中断恢复均有测试 |
| 27 | ClaimVerification与质量门控 | 已完成 | 26 | 已实现 `internal/quality/verification.go` VerificationResult/ClaimVerdict（校验、按 claim merge、陈旧 verdict 剪枝、verdict 写回 ClaimNode 预留字段）、`internal/quality/gate.go` 三档门控矩阵（硬阻断四类底线所有模式生效、分级风险按 fast/enhanced/strict 升级、重写超 2 轮 needs_human_review 且 strict 置顶、`CombineConclusions` 与 artifacts 章节门控并联）、`save_verification_result`/`quality_gate_check` 工具与 `internal/agent` `claim_verification` step 接线；门控矩阵逐行测试、硬阻断四类逐项测试、mock Coordinator checkpoint 写入与恢复均有测试 |
| 28 | EditorRewriteInstructions | 已完成 | 27 | 已实现 `contracts.Review.rewrite_instructions` 兼容扩展、`agent.GuardReviewInstructions`/`WriteGuardedReview` Host 校验（evidence id、claim id、unsupported/overstated/needs_rewrite 覆盖）、上一轮 instructions 注入 `WriterChapterInput`、required instruction 并入章节门控与超限人工复核；schema 兼容、非法 evidence/claim/severity、mock 重写闭环与超限均有测试 |
| 29 | QualityReport导出汇总 | 已完成 | 27, 28, 08 | 已实现 `export.QualityInput` 质量产物自动加载（缺失标记兼容模式）、`RenderQualityReport` 渲染 `final/quality-report.md`（门控/证据深度/支持度/重写/下一步五节）、report.md 质量摘要 section、质量报告失败不阻塞 paper.md/docx；渲染/导出/兼容/降级均有测试 |
| 30 | TUI质量模式与恢复兼容 | 已完成 | 29, 10-22 | 已实现 Requirements 表单 quality_mode 三档（默认 enhanced）、WritingProgress verifying/needs_revision 章节状态、ExportSummary 质量摘要与 quality-report 入口、StateProbe 质量产物与模式探测、RecoverPrompt 模式注明与兼容提示、Export Result Metadata 传递质量结论；state_probe_quality_test 覆盖质量探测场景 |
| 31 | 三层验收与质量夹具 | 已完成 | 23-30, 09 | 第一层 build+test 全通过并修复 1 个测试 HOME 隔离缺陷；第二层 fixtures/quality-mini + quality_mini_test.go 六类坏样本全断言（含 fast 模式硬阻断与降级）；第三层 mock 结构对照 + 真实 provider 对照（gpt-5.5 经 tools/real-before-after harness 补跑：before 8 claim 零绑定 vs after 9 claim 全绑定、verifier 抓出 4 处 partially_supported）；验收中修正模块 29 门控结论按章计数文案，并发现模块 22 TUI→AgentRuntime 接线缺口（见验收报告遗留事项 3，建议走 Bug修复）；报告见 `docs/QualityEngine验收报告.md` |

## 搜索优化任务（32）

> 规划日期：2026-06-17  
> 来源：用户反馈 - 中文主题搜索结果不相关  
> 状态：待开发（规划已完成）

| 编号 | 模块 | 状态 | 依赖 | 备注 |
|---|---|---|---|---|
| 32 | 智能查询生成策略实现 | 待开发 | 03 | 解决中文主题搜索不相关问题（如"明朝君主哪个更厉害"返回 90% 朝鲜史文献）；实现语言自适应查询、中文实体识别（朝代/人名/比较标记）、多策略查询生成、查询质量评分；目标：中文主题相关率从 ~10% 提升到 ≥70%；详见 `docs/superpowers/specs/2026-06-17-intelligent-query-generation.md` 与 `vault/32-智能查询生成策略实现.md` |

## 论文质量 Skill 运行时增量任务（39-45）

> 规划日期：2026-06-20  
> 来源：`docs/superpowers/specs/2026-06-20-paper-quality-skill-runtime-design.md`  
> 状态：✅ 已完成（39-45 实现与端到端验收通过 2026-06-20）

| 编号 | 模块 | 状态 | 依赖 | 备注 |
|---|---|---|---|---|
| 39 | PaperQualitySkill运行时设计与接口 | 已完成 | 23-31 | 运行时设计稿、`paper_quality_skill.md` 接口与 skill 蒸馏来源已落盘；明确不直接读取 docs/skills、不扩首期 schema |
| 40 | PaperQualityPolicy本地策略底座 | 已完成 | 39 | `internal/quality/paper_quality_policy.go` 纯函数策略 helper（8 scope + forbidden patterns + version 常量），无文件 IO；单测覆盖 scope/核心规则/稳定性 |
| 41 | Coordinator与Architect质量策略注入 | 已完成 | 40, 24 | Coordinator prompt 追加全局策略；Architect outline/evidence/section plan 注入 narrative/evidence depth/section plan rubric |
| 42 | Writer证据约束与风格策略注入 | 已完成 | 40, 25 | WriterChapterInput/Writer prompt 注入 evidence-grounded writing、gap 不进正文主体、style guard；writer guarded 条件收紧为 plan+table 即生效 |
| 43 | Verifier与Editor审稿Rubric注入 | 已完成 | 40, 27, 28 | `editor_run verify/review` 注入 claim type/support rubric、reviewer rubric、rewrite/human review 规则；Verifier 补漏判 claim，Editor 用 quality 结论覆盖通过的 review |
| 44 | QualityReport质量Skill摘要增强 | 已完成 | 40, 29 | QualityReport 展示 policy version、证据深度解释、Human Action Items；deterministic，无 LLM |
| 45 | 运行时启用端到端验收 | 已完成 | 41-44, 31 | 新增 2 个 E2E 验收测试（策略不退化硬阻断与三档、导出报告含 policy version 与 action items）；扩展 before/after 断言 policy version 与 action items；`go build ./cmd/aipaper-cli`、`go test ./...` 全通过 |

## Bug 修复任务

| 编号 | 任务 | 类型 | 状态 | 备注 |
|---|---|---|---|---|
| BUG-20260613-01 | 修复：真实 Runtime 编排从未接线（TUI 不启动 AgentRuntime，Writer/Editor 无 LLM runner，模块 22 完成记录失实） | bugfix | ✅ 已修复 | 方案 B（B1-B7）已落代码并验收（2026-06-13）：Writer/Editor/Architect LLM runner、architect_run/editor_run 工具、TUI runtime launcher、事件桥、停止与恢复注入；`go build`/`go test` 全过，mock 编排测试 + 真实 provider harness 回归（gpt-5.5：after 流 9 claims 全绑定、gate=pass_with_warnings）通过；TUI 交互冒烟由用户裁定免做；模块 22 不实记录已修正；详见 `vault/BUG-20260613-01-真实Runtime编排接线.md` |
| BUG-20260617-01 | 修复：Coordinator 不调用 editor_run commit 导致 accepted.md 缺失、导出失败 NO_ACCEPTED_CHAPTERS | bugfix | ✅ 已修复 | 方案 C（临时脚本 + 流程修复）已完成（2026-06-18）：临时脚本已修复当前产物（7 个章节 accepted.md 已创建）；流程修复已落代码（editor_runner.runReview 自动 commit）；编译和测试全通过（含幂等性验证）；commit 5398d00；详见 `vault/BUG-20260617-01-accepted缺失导出失败.md` |
| BUG-20260618-01 | 修复：文献引用不足（2篇）与 Docx 格式全丢失 | bugfix | ✅ 已修复 | 已完成代码层修复（2026-06-18）：搜索参数提升为 Limit=20、MinCandidates=30、Expansion=5；References TUI 默认至少确认 5 篇文献并显示错误提示；Docx 导出保留标题层级、段落间距/首行缩进、数字引用上标、粗体/斜体，并修复畸形 Markdown 死循环风险；`go test ./...` 与 `go build -o aipaper-cli.exe ./cmd/aipaper-cli` 通过；详见 `vault/BUG-20260618-01-文献数量不足与格式问题.md` |

## Paper Quality Skill 运行时验收总线

| 验收项 | 覆盖模块 | 状态 |
|---|---|---|
| Skill 内容已本地化为无文件 IO、可测试的运行时策略 | 39, 40 | 已完成（40 纯函数 policy helper，单测覆盖） |
| Coordinator/Architect/Writer/Verifier/Editor prompt 均注入对应质量策略 | 41-43 | 已完成（41-43 注入 + `role_runner_test`/`prompt_test` 各角色 prompt 断言） |
| 证据不足不会成为正文主体，必须进入 notes、rewrite 或 human review | 42, 43 | 已完成（42 forbidden patterns + writer_notes；43 unsupported/overstated 必给 rewrite instruction，不可修标 human review） |
| QualityReport 能解释 policy version、证据深度、claim 风险和人工行动项 | 44 | 已完成（44 渲染 + E2E 报告断言） |
| 旧项目兼容模式、fast/enhanced/strict 与既有硬阻断不退化 | 45 | 已完成（45 E2E：硬阻断四类全模式 blocked、策略注入不改门控矩阵、before/after 兼容模式与三档对照） |

## Quality Engine 验收总线

| 验收项 | 覆盖模块 | 状态 |
|---|---|---|
| 质量产物完整、可恢复、可校验 | 23-27 | 已完成（23 EvidenceTable、24 SectionQualityPlan、25 Writer 证据协议与 claims 兼容读取、26 ClaimGraph 按章增量更新与恢复、27 VerificationResult 按 claim merge 与恢复均已实现并有测试） |
| 底线问题必被硬阻断 | 25, 27, 31 | 已完成（25 writer guard 三类硬阻断；27 quality_gate_check 四类底线所有模式 blocked；31 quality-mini 夹具断言阻断后产物不落盘、gate 层全模式 blocked） |
| 三档模式门控行为正确 | 27, 30, 31 | 已完成（27 门控矩阵逐行测试；30 TUI 模式选择落盘；31 夹具同输入跨模式对照：enhanced=warning/strict=needs_revision/fast=warnings-only） |
| 旧项目恢复不被破坏 | 29, 30 | 已完成（29 兼容模式导出不阻塞且不生成 quality-report；30 StateProbe/RecoverPrompt 兼容提示；31 before 流验证旧产物导出完整） |
| 用户可感知质量提升 | 31 | mock 结构对照 + 真实 provider 对照（gpt-5.5）均已完成：evidence 绑定 0→全绑定、真实 verifier 逐条标记 partially_supported、quality-report 生成；用户已签字通过（ZHIYU，2026-06-13） |

## TUI 增量验收总线

| 验收项 | 覆盖模块 | 状态 |
|---|---|---|
| 无参数启动进入 TUI，带参数 CLI 保持兼容 | 10 | 已完成 |
| ConfigWizard 模板配置可被 config/runtime 解析且 key 脱敏 | 11 | 已完成 |
| 启动状态探测和 checkpoint 恢复提示正确 | 12, 18 | 已完成（12 状态探测、18 RecoverPrompt 与停止恢复均完成） |
| Requirements 到 MaterialsScan 桥接落盘正确 | 13, 14 | 已完成 |
| 材料 BibTeX 候选与搜索候选合并不丢失 | 14, 15 | 已完成 |
| 文献确认仍是写作硬门槛 | 16 | 已完成 |
| WritingProgress 能显示 streaming、usage、日志和章节进度 | 17 | 已完成（mock 测试验证） |
| Ctrl+C 安全停止并支持下次恢复 | 18 | 已完成（两阶段停止 + RecoverPrompt 恢复，mock 测试验证） |
| ExportSummary 展示实际 final outputs 与 docx 降级 | 19 | 已完成 |
| 用户文档与 README 命令、路径一致 | 20 | 已完成 |
| Mock 全流程、真实 provider smoke 记录、Windows 双击验收 | 21 | 自动化验收已完成（mock 全流程和构建/CLI smoke 通过；真实 provider 因无 API key 跳过；桌面双击交互后续已由模块 22 补齐记录，非交互控制台错误已记录为可读） |
| 真实 runtime 接入、stop adapter、真实 provider smoke、Windows 本机双击确认 | 22 | 经 BUG-20260613-01 补实（原"已完成"记录失实）：真实 Host/AgentRuntime 启动、Ctrl+C Abort 停止、恢复 prompt 注入、流式/usage 事件桥均已落代码并有 mock 编排测试；真实 provider 冒烟与 Windows 本机双击验收状态见 `vault/BUG-20260613-01-真实Runtime编排接线.md` 四步记录 |

## 验收总线

| 验收项 | 覆盖模块 | 状态 |
|---|---|---|
| TUI 表单到最终输出目录完整闭环 | 02-08 | 已完成 |
| 文献确认是硬性步骤，未确认文献不可引用 | 03, 04, 06 | 已完成 |
| Editor 评审结构稳定并触发重写/人工复核 | 06 | 已完成 |
| Step checkpoint 可校验 artifact hash 并恢复 | 01, 07 | 已完成 |
| 输出 `paper.md`、`paper.docx`、`report.md` 可用 | 08 | 已完成 |
| E2E fixture 可跑通并覆盖中途恢复 | 09 | 已完成 |

## 全量验收复核

- 复核日期：2026-06-07
- `go build ./...`：通过（exit 0）
- `go test ./...`：全部通过
- 结论：所有模块（01-09）构建与测试均正常，代码与进度声明一致，无阻塞问题。
- 阶段四复核已完成
