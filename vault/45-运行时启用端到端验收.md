# 45-运行时启用端到端验收

**状态**: 已完成（端到端验收通过 2026-06-20）

## 任务目标

验证 Paper Quality Skill 内容已经在运行时启用，并且覆盖大纲、证据、章节计划、写作、验证、审稿、导出报告全过程，同时不破坏既有质量引擎和兼容模式。

## 前置依赖

- 41-Coordinator与Architect质量策略注入
- 42-Writer证据约束与风格策略注入
- 43-Verifier与Editor审稿Rubric注入
- 44-QualityReport质量Skill摘要增强
- 31-三层验收与质量夹具

## 需要读取

- `internal/quality/*_test.go`
- `internal/agent/*_test.go`
- `internal/app/role_runner_test.go`
- `internal/export/quality_report_test.go`
- `internal/e2e/quality_mini_test.go`
- `internal/e2e/before_after_test.go`

## 实现要求

1. 补齐必要单元测试和集成测试。
2. 确认 Coordinator/Architect/Writer/Verifier/Editor prompt 均有策略注入证据。
3. 确认 QualityReport 输出策略版本和人工行动项。
4. 确认旧项目兼容模式、fast/enhanced/strict 和硬阻断不退化。

## 测试要求

- `go test ./internal/quality ./internal/agent ./internal/app ./internal/export`
- `go test ./internal/e2e/...`
- `go test ./...`
- `go build ./cmd/aipaper-cli`

## 验收标准

- 全量测试与构建通过。
- 质量策略注入有自动化断言。
- 不放宽 Writer Guard、Editor Guard、Quality Gate 既有断言。

## 已知风险/边界

- 真实 provider 冒烟依赖 API key，可作为人工/环境验收，不作为本任务硬依赖。
