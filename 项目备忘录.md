# 项目备忘录

## 项目概述

`aipaper-cli` 是面向学术综述 / 文献综述写作的 AI Agent CLI：通过 TUI 收集写作需求和文献确认，由 Coordinator 调度 Architect、Writer、Editor 完成大纲、章节草稿、评审、重写、导出，并保留可追踪、可恢复的结构化产物。

## 技术栈

- 语言：Go 1.25
- Agent：`github.com/voocel/agentcore` v1.6.11
- LLM 适配：`github.com/voocel/litellm` v1.6.15
- TUI：Bubble Tea、Bubbles、Lip Gloss
- Store：本地文件系统，`output/aipaper/`
- 写入策略：temp + fsync + rename，JSON 独立校验

## 核心约定

- Store 根目录：`output/aipaper/`
- 时间格式：Go `time.Time` 序列化为 RFC3339 UTC
- 路径格式：checkpoint artifact 路径必须是相对 Store 根的正斜杠路径
- ID 策略：run 使用时间戳 ID；材料用 `material_001`；候选文献用 `cand_001`；引用 key 用 `firstAuthorYearShortTitle`
- 错误处理：工具失败返回 `{ok:false,error:{code,message,retryable,details}}`
- 引用硬规则：Writer 和 Editor 只能使用 `references/confirmed.json`
- 质量门控：单章总分 >= 80，引用一致性 >= 90，无高风险 unsupported claim；最多重写 2 轮
- 质量模式：`requirements.json.quality_mode = fast | enhanced(默认) | strict`，三档只改门控严格度，产物结构相同；硬阻断（伪造 key、claim 无 evidence、引用未 confirmed）所有模式生效
- 质量产物：`quality/evidence-table.json`、`quality/section-quality-plan.json`、`quality/claim-graph.json`，契约见 `docs/interfaces/quality.md`

## 模块依赖图

```text
01-基础脚手架与配置Store
  ├─ 02-材料解析与索引
  │    └─ 04-文献确认TUI
  ├─ 03-学术搜索与文献候选
  │    └─ 04-文献确认TUI
  ├─ 07-StepCheckpoint恢复
  └─ 05-Agent运行时与Coordinator
       └─ 06-写作产物与质量门控
            └─ 08-最终导出与报告
                 └─ 09-E2E验收与测试夹具

10-无参数TUI启动与RootModel骨架
  ├─ 11-ConfigWizard配置向导
  └─ 12-启动状态探测与恢复入口
       └─ 13-Requirements屏幕桥接与落盘
            └─ 14-MaterialsScan与候选材料入口
                 └─ 15-SearchProgress与候选合并
                      └─ 16-References屏幕桥接与确认落盘
                           └─ 17-WritingProgress四区布局与Runtime事件桥
                                ├─ 18-暂停退出与安全恢复
                                └─ 19-ExportSummary与完成页
                                     └─ 20-用户文档与README快速入口
                                          └─ 21-TUI全流程测试与Windows双击验收
                                               └─ 22-真实Runtime接入与本机验收

23-EvidenceTable底座（依赖 01, 02, 04）
  └─ 24-SectionQualityPlan写前约束（依赖 05, 06）
       └─ 25-Writer证据使用协议
            └─ 26-ClaimGraph写后抽取
                 └─ 27-ClaimVerification与质量门控
                      └─ 28-EditorRewriteInstructions
                           └─ 29-QualityReport导出汇总（依赖 08）
                                └─ 30-TUI质量模式与恢复兼容（依赖 10-22）
                                     └─ 31-三层验收与质量夹具（依赖 09）
```

## 文档指针

- 已批准设计稿：`docs/superpowers/specs/2026-06-05-aipaper-cli-design.md`
- TUI 全流程增量设计稿：`docs/superpowers/specs/2026-06-08-tui-full-generation-user-guide-design.md`
- Quality Engine 设计稿：`docs/superpowers/specs/2026-06-10-quality-engine-design.md`
- 合并需求与架构：`docs/需求与架构.md`
- TUI 增量需求：`docs/TUI全流程增量需求.md`
- TUI 增量架构：`docs/TUI全流程增量架构设计.md`
- TUI 增量开发流程：`docs/TUI全流程详细开发流程.md`
- 原始需求提炼：`docs/项目需求.md`
- 架构设计草稿：`docs/架构设计.md`
- 接口与类型索引：`docs/interfaces/_index.md`
- Quality 接口契约：`docs/interfaces/quality.md`
- 开发进度：`docs/开发进度.md`
- 模块任务：`vault/*.md`
- 当前 TUI 增量任务：`vault/10-*.md` 至 `vault/22-*.md`（已完成）
- 当前 Quality Engine 任务：`vault/23-*.md` 至 `vault/31-*.md`（进行中：23、24、25、26、27 已完成）

## 勿动文件

- `internal/checkpoint/checkpoint.go`：checkpoint 记录、latest 校验、路径安全与 step 冲突语义已稳定。
- `internal/app/recover.go`：Host 恢复入口、恢复 prompt、`run.json.resumed_from` 更新已稳定。
- `internal/checkpoint/checkpoint_test.go`、`internal/app/recover_test.go`、`internal/cli/cli_test.go` 中的恢复相关用例：覆盖 07 模块验收，不应无故删除。
- `internal/agent/`：Coordinator prompt、step runner、事实工具、事件投影和结构化错误码已稳定；06 只应在其边界上新增写作产物逻辑。
- `internal/app/agent_runtime.go`：Host runtime builder、role/provider 解析、env secret 解析和 agentcore/litellm 接入已稳定。
- `internal/artifacts/`：写作产物路径、原子写入、质量门控、confirmed references 校验与章节状态机已稳定；08/09 应复用这些接口，不应重复实现产物规则。
- `internal/export/`：最终 Markdown、Docx、references、citation trace、report 导出接口已稳定；09 应通过 `LoadInput`/`ExportFinal` 做端到端验收。
- `fixtures/review-mini/` 与 `internal/e2e/review_mini_test.go`：09 冒烟验收夹具，覆盖无网络/无真实 LLM 的 MVP artifact 闭环和中途恢复。
- `internal/tui/requirements/`：需求表单 model/view/update 与校验已稳定；后续 Bubble Tea runtime 应桥接该 model，不应重写需求校验规则。
- `internal/tui/references/`：文献确认 model/view/update、搜索/排序/拒绝/确认行为已稳定；后续 RootModel 应桥接，不应重写 key 冲突和确认落盘规则。
- `internal/quality/evidence.go`：EvidenceTable 类型、校验规则（confirmed reference、depth 渐进、ID 风格）与原子读写已稳定；24-31 在包内新增文件扩展，不应修改既有校验语义与错误码。
- `internal/quality/sectionplan.go`、`internal/quality/sectionplan_tools.go`：SectionQualityPlan 类型、校验规则（chapter_id 与 outline 一致、required_evidence_ids 存在于 Evidence Table）、原子读写与 save/load 工具已稳定；25-31 只应消费其接口，不应修改校验语义与错误码。`internal/agent.DefaultTools` 已追加 `quality.Tools(s)`，后续 quality 工具加入 `quality.Tools` 即自动注册。
- `internal/agent/writer_quality.go`、`internal/artifacts/claims.go`：Writer 证据协议（`WriterChapterInput` 注入、`GuardWriterClaims`/`WriteGuardedDraftBundle` 硬校验、`WRITER_CLAIM_*` 错误码）与 claims 兼容 warning（`ARTIFACT_CLAIM_MISSING_EVIDENCE`）已稳定；26-31 只应消费这些接口，不应放宽硬校验或修改错误码。
- `internal/quality/claimgraph.go`、`internal/quality/claimgraph_tools.go`、`internal/agent/claim_quality.go`：ClaimGraph 类型、校验、章节级增量 merge 与跨章重复检测、`save_claim_graph`/`load_claim_graph`/`extract_chapter_claims` 工具与 `claim_extraction` step 常量已稳定；27-31 只应消费其接口（27 在 Support/RiskLevel/VerifierNote 等预留字段上写 verification 结果），不应修改校验语义、错误码与 merge 规则。
- `internal/quality/verification.go`、`internal/quality/gate.go`、`internal/quality/verification_tools.go`、`internal/agent/verification_quality.go`：VerificationResult/ClaimVerdict（校验、按 claim merge 与剪枝、verdict 写回 ClaimNode）、三档门控矩阵（硬阻断四类所有模式生效、分级风险按模式升级、重写超限 needs_human_review、`CombineConclusions`/`ChapterGateConclusion` 并联既有章节门控）、`save_verification_result`/`quality_gate_check` 工具与 `claim_verification` step 常量已稳定；28-31 只应消费其接口（28 消费 verdict 与 gate findings 生成 rewrite instructions，29 消费 GateOutcome 汇总 report），不应修改门控矩阵、错误码与 merge/剪枝规则。

## Git Commit 规范

- 默认在当前分支线性开发
- 格式：`feat/fix/test/refactor/docs/chore(scope): description`
- 每完成一个功能点或一个可独立验证的文档变更提交一次

## Agent 调度要点

- 每个开发任务开始前先读本文件、`docs/需求与架构.md`、对应 `vault/XX-*.md`
- TUI 增量任务（10-21）额外先读 `docs/TUI全流程增量需求.md`、`docs/TUI全流程增量架构设计.md`、`docs/interfaces/tui.md`
- Quality Engine 任务（23-31）额外先读 `docs/superpowers/specs/2026-06-10-quality-engine-design.md`、`docs/interfaces/quality.md`；勿动目录只做边界扩展，不重写既有规则
- 注意：`项目备忘录.skill` 为开发阶段权威备忘录（含完整 TUI 勿动清单），本文件与其保持同步
- 代码实现优先复用现有 `internal/contracts`、`internal/store`、`internal/checkpoint`、`internal/config`
- Host 只做可靠执行、事件投影、工具注册和恢复，不把写作调度规则写死在 Host
- Coordinator 是唯一流程决策者，决策必须基于工具返回的结构化事实
