# 修复记录

| 日期 | 模块 | 类型 | 问题描述 | 根因分析 | 修复方式 | 防范措施 |
|---|---|---|---|---|---|---|
| 2026-06-10 | tui/root | 单模块缺陷 | MaterialsScan 继续后仅切换到 SearchProgress，未初始化 Search model 与启动搜索命令 | key-action 转场绕过了 `ScreenTransitionMsg` 的初始化路径 | 在 `updateMaterials` 中构造 `newSearchModel`，保存 `ScreenData`，并返回 `m.Search.Init()`；新增 RootModel 测试覆盖材料候选传递和禁用搜索路径 | RootModel 新增屏幕转场时必须同时覆盖消息转场与按键转场路径 |
| 2026-06-10 | tui/root | 单模块缺陷 | SearchProgress 继续后仅切换到 References，未加载候选到 References model | key-action 转场未复用 References 初始化逻辑 | 在 `updateSearch` 中调用 `newReferencesModel` 并写入 `m.References`；新增测试验证 References 视图能看到候选 | 对所有跨屏按键路径增加“目标 model 已初始化”的断言 |
| 2026-06-10 | tui/search | 单模块缺陷 | `allow_online_search=false` 时仍会调用搜索 provider | SearchProgress 在 `Init` 阶段无条件执行 `searchCmd`，只在结果处理后标记 Disabled | 增加 `disabledSearchCmd`，禁用在线搜索时只合并材料候选并写 candidates，不触发 provider；新增“不调用 provider”测试 | 后续涉及外部调用必须先判断用户配置开关，再构造调用命令 |
| 2026-06-10 | tui/app | 单模块缺陷 | 恢复继续后进入 Writing 但未创建 Writing model / 返回 Init 命令 | RecoverPrompt 的继续路径只设置 `CurrentScreen` 与 `ScreenData` | 在恢复成功后构造 `newWritingModel` 并返回 `m.Writing.Init()` | 恢复/重启类转场需验证目标屏 model 与 init 命令 |
| 2026-06-10 | tui/state-probe | 单模块缺陷 | 跳过材料后已存在 candidates 时，重启仍优先回到 MaterialsScan | StateProbe 在检查 candidates 前强制要求 HasMaterials | 调整路由顺序：未确认引用时优先使用已有 candidates 进入 References，否则有材料进入 SearchProgress，二者都无才回 MaterialsScan；新增测试覆盖无材料但有 candidates 的恢复 | StateProbe 路由需按“已有权威产物优先”原则排序 |
| 2026-06-10 | tui/state-probe | 单模块缺陷 | 配置探测重复实现 runtime 解析规则，可能与真实运行时不一致 | `validateTUIRuntimeConfig` 手写 role/provider 校验，未复用 runtime 解析与 secret 解析 | 改为对 coordinator/architect/writer/editor 调用 `runtimeapp.ResolveRoleRuntime` | 配置有效性判断复用运行时同一入口，避免漂移 |
