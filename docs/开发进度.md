# 开发进度

> ✅ Quality Engine（23-31）阶段四复核已完成（2026-06-10），可进入开发阶段。

## 状态说明

- 待开发：尚未进入实现
- 进行中：已有部分代码或文档，但未完成验收
- 已完成：实现、测试和复核均通过
- 阻塞：需要用户决策或外部条件

## 模块任务

| 编号 | 模块 | 状态 | 依赖 | 备注 |
|---|---|---|---|---|
| 01 | 基础脚手架与配置Store | 已完成 | 无 | 已补 CLI、config、store、bootstrap 单元测试；重复 init、配置脱敏、CreateOnly、严格 JSON 读取均已覆盖 |
| 02 | 材料解析与索引 | 已完成 | 01 | 已实现材料扫描、manifest/extracted/parsed 写入、MD/TXT/BibTeX/PDF 解析、DOCX/URL/CSV 降级解析与 fixtures 测试 |
| 03 | 学术搜索与文献候选 | 已完成 | 01 | 已实现 Semantic Scholar/Crossref/arXiv/PubMed provider、HTTP mock 测试、标准化、三层去重、候选 JSON/Markdown 写入 |
| 04 | 文献确认TUI | 已完成 | 02, 03 | 已实现可测试 TUI model、选择/搜索/排序/拒绝/确认、confirmed/rejected/BibTeX 原子写入与 key 冲突处理 |
| 05 | Agent运行时与Coordinator | 已完成 | 01, 04, 07 | 已接入 agentcore/litellm，完成 Host runtime builder、Coordinator prompt/step、事实工具、事件投影、writer guard 与 mock 测试 |
| 06 | 写作产物与质量门控 | 已完成 | 05 | 已实现 `internal/artifacts` 路径/写入、质量门控、章节状态机、confirmed references 校验与单元测试 |
| 07 | StepCheckpoint恢复 | 已完成 | 01 | 已实现 latest 校验、路径/哈希失败报告、恢复 prompt、run.json.resumed_from 更新、CLI recover JSON 输出与测试 |
| 08 | 最终导出与报告 | 已完成 | 06, 07 | 已实现 `internal/export` LoadInput/ExportFinal、Markdown/Docx/references/trace/report 输出、Docx 降级和未确认引用拦截 |
| 09 | E2E验收与测试夹具 | 已完成 | 02, 03, 04, 05, 06, 07, 08 | 已新增 review-mini fixture 与 `internal/e2e` 冒烟测试，覆盖材料解析、mock 搜索、确认、重写、恢复、导出和 trace 校验 |

## TUI 全流程增量任务（10-21）

> 规划日期：2026-06-08  
> 来源：`docs/superpowers/specs/2026-06-08-tui-full-generation-user-guide-design.md`  
> 状态：✅ 增量规划已生成，待进入开发阶段

| 编号 | 模块 | 状态 | 依赖 | 备注 |
|---|---|---|---|---|
| 10 | 无参数TUI启动与RootModel骨架 | 已完成 | 01 | 已实现 `main.go` 无参数进 TUI，带参数 CLI 不变，RootModel 转场和 TUI 错误路径已覆盖 |
| 11 | ConfigWizard配置向导 | 已完成 | 10 | 已实现 OpenAI/Anthropic/Ollama/Custom 模板、项目级 `aipaper.json` 保存、role runtime 解析与 API key 脱敏 |
| 12 | 启动状态探测与恢复入口 | 已完成 | 07, 10, 11 | 已实现配置、checkpoint/run、requirements、materials、references、final 状态探测和恢复提示意图 |
| 13 | Requirements屏幕桥接与落盘 | 已完成 | 10, 12 | 已桥接既有 `internal/tui/requirements`，完成需求校验、`requirements.json` 落盘、默认 `materials/` 创建和 MaterialsScan 转场 |
| 14 | MaterialsScan与候选材料入口 | 已完成 | 02, 13 | 已实现 `internal/tui/materials` 扫描屏、缺/空目录提示、失败/降级详情、BibTeX 候选保留和 RootModel 转场 |
| 15 | SearchProgress与候选合并 | 已完成 | 03, 14 | 已实现搜索屏、材料+搜索候选合并去重、ID 重分配、最终 candidates 写入与 RootModel 转场 |
| 16 | References屏幕桥接与确认落盘 | 已完成 | 04, 15 | 已桥接既有 `internal/tui/references`，完成 LoadCandidates/ConfirmCandidates 调用、confirmed/rejected/BibTeX 写入、未确认阻塞和 WritingProgress 转场 |
| 17 | WritingProgress四区布局与Runtime事件桥 | 已完成 | 05, 06, 07, 16 | 已实现 `internal/tui/writing` 包、RuntimeEvent 桥接、四区布局 view、mock 事件测试；真实 runtime 接入已由模块 22 补齐 |
| 18 | 暂停退出与安全恢复 | 已完成 | 07, 12, 17 | 已实现 WritingProgress 两阶段停止（Ctrl+C→stopping→checkpoint saved→canceled）、RecoverPrompt 继续/重开/退出交互（重开二次确认且不删文件）、RootModel 退出确认；32 个单元测试全过 |
| 19 | ExportSummary与完成页 | 已完成 | 08, 17 | 已实现 ExportSummary/Done 包、复用 export.LoadInput/ExportFinal、RootModel 转场、37 个单元测试全过（覆盖率 98.4%/100%） |
| 20 | 用户文档与README快速入口 | 已完成 | 10-19 | 已新增 `docs/user-guide.md`（9 章 + 环境变量附录）、README 精简为快速入口；41 个文档一致性测试全过（CLI 命令、导出路径、环境变量、链接、默认模型均与代码实现一致） |
| 21 | TUI全流程测试与Windows双击验收 | 已完成 | 10-20 | 已新增 `internal/e2e/tui_flow_test.go`，覆盖 TUI mock 全流程状态契约、ConfigWizard runtime 配置解析、文档一致性；`go test ./...`、`go build ./cmd/aipaper-cli`、`go build -o aipaper-cli.exe ./cmd/aipaper-cli` 全部通过；真实 provider smoke 因无 API key 跳过并记录 |
| 22 | 真实Runtime接入与本机验收 | 已完成 | 05, 07, 17, 18, 21 | 已完成 WritingProgress 真实 Host/AgentRuntime 启动路径、真实 runtime stop adapter、恢复 prompt 注入、真实 provider smoke 记录与 Windows 本机验收记录 |

## Quality Engine 质量引擎任务（23-31）

> 规划日期：2026-06-10  
> 来源：`docs/superpowers/specs/2026-06-10-quality-engine-design.md`  
> 状态：✅ 规划已生成，阶段四复核已完成（2026-06-10），可进入开发阶段

| 编号 | 模块 | 状态 | 依赖 | 备注 |
|---|---|---|---|---|
| 23 | EvidenceTable底座 | 已完成 | 01, 02, 04 | 已实现 `internal/quality` 包：Evidence 类型/原子存储/严格读取、confirmed reference 与 depth 渐进校验、save/load 工具与结构化错误码、Markdown 快照测试；工具注册接线留给模块 24 |
| 24 | SectionQualityPlan写前约束 | 已完成 | 23, 05, 06 | 已实现 `internal/quality/sectionplan.go` 类型/校验/原子存储、save/load_section_quality_plan 工具、`internal/agent` 边界接线（DefaultTools 注册 quality 工具、`evidence_extraction`/`section_quality_plan` step 常量与 Coordinator/Architect 提示词扩展）；mock Coordinator 两个新 step 的 checkpoint 写入、中断恢复续跑与幂等回放均有测试 |
| 25 | Writer证据使用协议 | 待开发 | 24, 06 | claims.json 增 `evidence_ids`、Writer 输入 quality plan、writer guard 硬校验 |
| 26 | ClaimGraph写后抽取 | 待开发 | 25 | `claim_extraction` step、按章增量更新、跨章重复检测 |
| 27 | ClaimVerification与质量门控 | 待开发 | 26 | `claim_verification` step、`quality_gate_check` 三档模式门控矩阵、硬阻断四类底线 |
| 28 | EditorRewriteInstructions | 待开发 | 27 | review.json 增结构化 rewrite instructions、重写闭环驱动 |
| 29 | QualityReport导出汇总 | 待开发 | 27, 28, 08 | `final/quality-report.md`、report.md 质量摘要、兼容模式导出 |
| 30 | TUI质量模式与恢复兼容 | 待开发 | 29, 10-22 | Requirements 三档模式、WritingProgress/ExportSummary 增强、StateProbe/RecoverPrompt 兼容 |
| 31 | 三层验收与质量夹具 | 待开发 | 23-30, 09 | quality-mini 夹具、门控矩阵 E2E、短综述 before/after 验收与文档同步 |

## Quality Engine 验收总线

| 验收项 | 覆盖模块 | 状态 |
|---|---|---|
| 质量产物完整、可恢复、可校验 | 23-26 | 进行中（23 EvidenceTable、24 SectionQualityPlan 写入/读取/校验与 checkpoint 恢复已完成） |
| 底线问题必被硬阻断 | 25, 27, 31 | 待开发 |
| 三档模式门控行为正确 | 27, 30, 31 | 待开发 |
| 旧项目恢复不被破坏 | 29, 30 | 待开发 |
| 用户可感知质量提升 | 31 | 待开发 |

## TUI 增量验收总线

| 验收项 | 覆盖模块 | 状态 |
|---|---|---|
| 无参数启动进入 TUI，带参数 CLI 保持兼容 | 10 | 已完成 |
| ConfigWizard 模板配置可被 config/runtime 解析且 key 脱敏 | 11 | 已完成 |
| 启动状态探测和 checkpoint 恢复提示正确 | 12, 18 | 已完成（12 状态探测、18 RecoverPrompt 与停止恢复均完成） |
| Requirements 到 MaterialsScan 桥接落盘正确 | 13, 14 | 已完成 |
| 材料 BibTeX 候选与搜索候选合并不丢失 | 14, 15 | 已完成 |
| 文献确认仍是写作硬门槛 | 16 | 已完成 |
| WritingProgress 能显示 streaming、usage、日志和章节进度 | 17 | 已完成（mock 测试验证） |
| Ctrl+C 安全停止并支持下次恢复 | 18 | 已完成（两阶段停止 + RecoverPrompt 恢复，mock 测试验证） |
| ExportSummary 展示实际 final outputs 与 docx 降级 | 19 | 已完成 |
| 用户文档与 README 命令、路径一致 | 20 | 已完成 |
| Mock 全流程、真实 provider smoke 记录、Windows 双击验收 | 21 | 自动化验收已完成（mock 全流程和构建/CLI smoke 通过；真实 provider 因无 API key 跳过；桌面双击交互后续已由模块 22 补齐记录，非交互控制台错误已记录为可读） |
| 真实 runtime 接入、stop adapter、真实 provider smoke、Windows 本机双击确认 | 22 | 已完成（WritingProgress 已接入真实 Host/AgentRuntime 路径，Ctrl+C stop adapter 与 recovery prompt 注入已补齐；真实 provider smoke 与 Windows 本机验收结果已记录） |

## 验收总线

| 验收项 | 覆盖模块 | 状态 |
|---|---|---|
| TUI 表单到最终输出目录完整闭环 | 02-08 | 已完成 |
| 文献确认是硬性步骤，未确认文献不可引用 | 03, 04, 06 | 已完成 |
| Editor 评审结构稳定并触发重写/人工复核 | 06 | 已完成 |
| Step checkpoint 可校验 artifact hash 并恢复 | 01, 07 | 已完成 |
| 输出 `paper.md`、`paper.docx`、`report.md` 可用 | 08 | 已完成 |
| E2E fixture 可跑通并覆盖中途恢复 | 09 | 已完成 |

## 全量验收复核

- 复核日期：2026-06-07
- `go build ./...`：通过（exit 0）
- `go test ./...`：全部通过
- 结论：所有模块（01-09）构建与测试均正常，代码与进度声明一致，无阻塞问题。
- 阶段四复核已完成
