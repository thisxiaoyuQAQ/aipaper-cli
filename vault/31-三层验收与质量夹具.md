# 31-三层验收与质量夹具

**状态**: ✅ 已完成（before/after 主观签字待用户，见 `agent-output/request/QualityEngine-before-after-确认请求.md`）

## 任务目标

完成 Quality Engine 三层验收：自动化结构验收复核、证据链夹具验收（quality-mini）、短综述 before/after 验收。

## 前置依赖

- 23-30 全部 Quality Engine 模块
- 09-E2E验收与测试夹具

## 需要读取

- `项目备忘录.skill`
- `docs/interfaces/quality.md`
- `docs/superpowers/specs/2026-06-10-quality-engine-design.md`（第 7 节）
- `fixtures/review-mini/`、`internal/e2e/`

## 实现要求

### 1. 第一层：自动化结构验收复核

- 复核 23-30 模块测试覆盖：schema、原子写入、门控矩阵、checkpoint 恢复、TUI；
- `go build ./...`、`go test ./...`、`go build -o aipaper-cli.exe ./cmd/aipaper-cli` 全通过。

### 2. 第二层：证据链夹具（fixtures/quality-mini/）

新增夹具与 `internal/e2e/quality_mini_test.go`（全程 mock agent、无网络），埋入坏样本并断言：

| 埋入问题 | 期望行为 |
|---|---|
| draft 引用未确认文献 key | Host 硬阻断 |
| claim 完全没绑定 evidence | Host 硬阻断 |
| 伪造引用 key | Host 硬阻断 |
| abstract 级证据 + 绝对化结论 | enhanced → warning，strict → needs_revision |
| 同一论断两章重复 | 分级风险标记 |
| 重写 2 轮仍不达标 | needs_human_review，流程不中断 |

同时覆盖 fast 模式：上述硬阻断仍生效、分级风险降为 warnings-only。

### 3. 第三层：短综述 before/after

- 扩充 `fixtures/materials` 真实材料，2-3 章、低字数；
- 兼容旧流程与 enhanced 模式各跑一次，对照记录到 `docs/QualityEngine验收报告.md`：
  - 大纲约束差异、正文论证差异、claim 支撑率、unsupported claim 数量、report 可读性；
- 有真实 API key 时跑真实 provider；无 key 时 mock 跑结构对照并记录跳过原因；
- 主观部分输出 `agent-output/request/` 确认请求，由用户签字。

### 4. 文档同步

- 更新 `docs/开发进度.md`（模块 23-31 与验收总线）；
- 更新 `项目备忘录.skill`（模块依赖图、文档指针、勿动文件如有新增）；
- 更新 `docs/user-guide.md`（三档质量模式与 quality-report 说明）。

## 验收标准

- 三层验收全部完成或如实记录跳过原因；
- spec 7.4 验收总线五项全部有证据；
- 用户确认 before/after 主观验收。

## 已知风险/边界

- 真实 provider smoke 须确认授权，避免未授权外部调用与费用；
- before/after 主观评估由用户最终签字，不由 Agent 自评通过。

## 行数预估

- `fixtures/quality-mini/` 数据文件若干 + `internal/e2e/quality_mini_test.go` ≈ 450 行；验收报告为文档产出；单文件 < 500 行。
